<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sistema</title>
    <script src="~/Scripts/Funciones_Json.js"></script>
    <!-- Bootstrap 3.3.6 -->
    <link rel="stylesheet" href="~/Estilo/bootstrap/css/bootstrap.min.css">
    <!-- Font Awesome -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.5.0/css/font-awesome.min.css">
    <!-- Ionicons -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/ionicons/2.0.1/css/ionicons.min.css">
    <!-- DataTables -->
    <link rel="stylesheet" href="~/Estilo/plugins/datatables/dataTables.bootstrap.css">
    <!-- Theme style -->
    <link rel="stylesheet" href="~/Estilo/dist/css/AdminLTE.min.css">
    <!-- AdminLTE Skins. Choose a skin from the css/skins
         folder instead of downloading all of them to reduce the load. -->
    <link rel="stylesheet" href="~/Estilo/dist/css/skins/_all-skins.min.css">
    <script src="https://unpkg.com/axios/dist/axios.min.js"></script>
    <script type="text/javascript">
        var inicio = false,st=true;
        function botones(boton) {
            switch (boton) {
                case "inicio":
                    inicio = true;
                    document.getElementById("but").innerHTML = "<a class='btn btn-warning' onclick='botones(\"pausar\")'> <i class='fa fa-hourglass-start'></i> Pausar Evento</a>";
                    if (st) {
                        Ost().then((response) => {
                            Age();
                        });
                
                        st = false;
                        
                    }
                    break;

                case "pausar":
                    inicio = false;
                    document.getElementById("but").innerHTML = "<a class='btn btn-success' onclick='botones(\"inicio\")'> <i class='fa fa-hourglass-start'></i> Iniciar Evento</a><a class='btn btn-danger' onclick='botones(\"restablecer\")'> <i class='fa fa-hourglass-start'></i> Restablecer Evento</a>";
                    break;

                case "restablecer":
                    Res().then((response) => { });
                    window.location = "Index";
                    break;

            }
            
        }
    </script>
    <script type="text/javascript">
        var int = self.setInterval("refresh()", 3000);
        function refresh() {
            if (inicio) {
                Esc();
            }
        }
        
        
    </script>

</head>
<body onload="Esc();">
    @RenderBody()
</body>
</html>